# App runtime dependencies; keep this file beside streamlit_app.py.
# Use prebuilt packages to avoid CPU-intensive compilation on Community Cloud.
--only-binary=:all:
streamlit==1.63.0
networkx==3.6.1
# pandas 2.3.3 adds Python 3.14 support.
pandas==2.3.3
plotly==7.0.0
numpy==2.3.5
